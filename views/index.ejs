<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>s - A simple http server</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        html {
            font-size: 62.5%;
        }

        body {
            font-size: 1.6rem;
            font-family: 'Lato', sans-serif;
            font-weight: 400;
            line-height: 1.2;
            color: #777;
        }

        .header {
            background: linear-gradient(to right, rgba(0, 0, 0, 0.267), rgba(0, 0, 0, 0.281)), linear-gradient(to right, #43C6AC, #191654);
            height: 35vh;
            color: white;
            font-size: 10rem;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: 100;
        }

        section {
            display: flex;
            flex-direction: column;
            margin-top: 1rem;
        }

        h1 {
            font-size: 10rem;
            font-weight: 100;
            padding: 2rem 0;
        }

        .item {
            padding: 1rem 2rem;
            border-bottom: 1px solid #beb7b7;
            display: flex;
            align-items: center;
        }

        a:visited,
        a:link {
            margin: 0 1rem;
            color: black;
            width: 100%;
        }

        a:hover {
            color: #999;
        }

        @media only screen and (max-width: 687px) {
            h1 {
                font-size: 5rem;
            }
        }
    </style>
</head>

<body>
    <header class="header">
        S - A simple HTTP Server
    </header>
    <section>
        <% allDirectories.forEach((directory) => { %>
        <div class="item">
            <span class="material-icons">folder_open</span>
            <a href="<%= directory %>"><%= directory %></a>
        </div>
        <% }) %>

        <% allFiles.forEach((file) => { %>
        <div class="item">
            <span class="material-icons">description</span>
            <a href="<%= file %>" download="<%= file %>"><%= file %></a>
        </div>
        <% }) %>

    </section>
</body>

</html>